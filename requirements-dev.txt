-r requirements.txt

# Para correr la suite de tests y medir cobertura. No van en requirements.txt
# porque no hacen falta para correr el producto, solo para desarrollarlo.
pytest>=8.0
coverage>=7.4
# Los tests leen render.yaml y los workflows como YAML de verdad, no como
# texto: chequear "está la palabra" pasa igual si la clave quedó en el nivel
# equivocado del árbol, que es exactamente el error que rompe un despliegue.
PyYAML>=6.0

# Opcionales, cada uno con su propio salteo si falta (ningún test se rompe
# sin esto, se saltea con motivo explícito):
#   - Cython + un compilador de C (gcc/cc del sistema): prueba real de
#     packaging/proteger_codigo.py.
#   - node en el PATH: corre el JS real de escaparHtml() en vez de confiar
#     en una reimplementación en Python.
Cython>=3.0

# Herramientas de build y de autoría. No las corre ningún cliente —por eso no
# van en requirements.txt— pero sí hacen falta para armar el instalador,
# grabar el video o doblarlo, y sin declararlas esos scripts fallan con
# ModuleNotFoundError en una máquina limpia.
#
#   packaging/generar_imagenes_instalador.py  ->  pillow
#   packaging/verificar_pantallas.py          ->  playwright
#   sitio/grabar_demo.py                      ->  playwright, imageio-ffmpeg
#   sitio/verificar_layout.py                 ->  playwright
#   sitio/doblar_video.py                     ->  imageio-ffmpeg (subtítulos y
#                                                 muxeo) y, solo con
#                                                 --motor local, chatterbox-tts
#                                                 con torch/torchaudio
pillow>=10.0
playwright>=1.40
imageio-ffmpeg>=0.4.9

# El doblaje de voz pesa varios GB y solo hace falta para regenerar el audio
# del video, así que se instala aparte a propósito:
#   pip install chatterbox-tts
# (trae torch y torchaudio). Los otros motores de sitio/doblar_video.py
# —voicebox y elevenlabs— no necesitan nada de esto.
